using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MO3 : CardFunction
{
    void Start()
    {
        
    }

    void Update()
    {
        //Card card = Instantiate(cardPrefab, new Vector3(0, 0, 0), Quaternion.identity, CardContent.transform).GetComponent<Card>();
    }
}
